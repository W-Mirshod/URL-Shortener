version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./static:/app/static
      - ./templates:/app/templates
      - ./url_shortener.db:/app/url_shortener.db
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

